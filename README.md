# Flask Restful Voluptuous Decorator
A decorator that can be used to validate JSON bodies being sent to Flask Restful methods.
### Installation
`pip install requirements.txt`
### Test
`nosetests app.py`
